# 11 - 看板与项目交接部署手册

> 伊菲学习管理系统 · 微信小程序 + 云开发（CloudBase）部署与日常维护
> 最后更新：2026-08-04（已实战跑通一次完整部署）

---

## 一、已部署现状（2026-08-04 实战确认）

| 项目 | 值 |
|---|---|
| 小程序名称 | 伊菲学习管理系统 |
| AppID | `wx5fa7812520b8392f` |
| 云开发环境 ID | `cloud1-d6gvwf6q09e5e6577`（个人版 / 上海） |
| 代码仓库 | https://github.com/haotvko/yifei-miniprogram.git （`master` 分支） |
| 云函数（5 个） | upload / getSnapshot / drawTask / getUploads / correctSubject（均已部署） |
| 数据库集合（2 个） | `snapshot`（存看板快照）、`uploads`（存作业上传记录） |

> ⚠️ 控制台里还有一个 `prod-d9g20vfkic3bb1e4e`（云托管，误建，不用管、可留着）。
> ⚠️ 那个 `prod-` 是「云托管 Cloud Run」，**不是**我们用的「云开发 CloudBase」，两者无关。

---

## 二、本地代码 → 云端的整体链路

```
Obsidian 00-最新状况.md（看板数据 YAML 块）
        │  python pipeline/export_snapshot.py
        ▼
pipeline/snapshot.json（看板快照）
        │  node pipeline/seed_db.js  （需 tcb CLI 已登录）
        ▼
云端数据库 snapshot 集合 / current 文档
        │  小程序调用 getSnapshot 云函数读取
        ▼
伊菲学习管理系统 App（掌握情况 / 今日任务 / 上传作业）
```

日常更新看板数据：**改 Obsidian → 跑 export_snapshot.py → 跑 seed_db.js**，App 下次打开即更新。

> 详情页两张派生卡的数据也由 `export_snapshot.py` 自动算出、写进云端 snapshot，无需手改：
> - 英语「词汇掌握 · 中考150分对照」→ `vocab_mastery_pct`（词汇辨析 + 词性转换名→形/动→名 三项掌握度均值）。
> - 数学「计算错误率 · 实时」→ `calc_error`（来自 `reports.json` 全部计算类考点的 wrong/total 汇总；`reports.json` 随每次作业分析更新，故错误率实时刷新）。
> 两字段缺省时对应卡片自动隐藏（语文/无数据不显示）。

---

## 三、环境准备（一次性）

1. 装 Node（本机已有，用 WorkBuddy 自带 managed 版本即可）。
2. 装 CloudBase CLI（只需在部署机器的 node 环境里装一次）：
   ```bash
   cd C:/Users/admin/.workbuddy/binaries/node/workspace
   npm install @cloudbase/cli
   ```
3. 登录 CLI（**设备码方式，最省事，不用扫二维码、不用在控制台翻菜单**）：
   ```bash
   node node_modules/@cloudbase/cli/bin/tcb login --flow device
   ```
   终端会打印一个授权网址 → 在自己电脑浏览器打开 → 点「确认授权」即可。
   （CLI 全局只登录一次，之后一直有效，直到 `tcb logout`。）

---

## 四、重新部署 / 更新（照抄即可）

### 1. 重新部署云函数（改了 `cloudfunctions/*` 后）
```bash
cd F:/workbuddy/2026-07-31-14-27-31/yifei-miniprogram
node "C:/Users/admin/.workbuddy/binaries/node/workspace/node_modules/@cloudbase/cli/bin/tcb" fn deploy --all --force -e cloud1-d6gvwf6q09e5e6577
```

### 2. 刷新看板数据（改了 Obsidian 后）
```bash
# 先确保 pipeline/snapshot.json 是最新的：
python pipeline/export_snapshot.py        # 需要 pyyaml + requests
# 再灌入云端：
node pipeline/seed_db.js
```

### 3. 查看云函数 / 集合状态
```bash
# 云函数列表
node .../tcb fn list -e cloud1-d6gvwf6q09e5e6577
# 查 snapshot 当前内容
node .../tcb db nosql execute -c '[{"TableName":"snapshot","CommandType":"QUERY","Command":"{\"find\":\"snapshot\",\"filter\":{},\"limit\":10}"}]' -e cloud1-d6gvwf6q09e5e6577
```

---

## 五、换机器 / 接手的人怎么开工

1. `git clone https://github.com/haotvko/yifei-miniprogram.git`
2. 装 CloudBase CLI 并 `tcb login --flow device` 登录（见第三节）。
3. 用微信开发者工具「导入项目」→ 目录选本仓库 → AppID 填 `wx5fa7812520b8392f`。
4. 开发者工具里点「云开发」→ 环境选 `cloud1-d6gvwf6q09e5e6577`。
5. 真机/模拟器打开「伊菲学习管理系统」即可看到数据。

---

## 六、关键纪律（来自项目铁律）

- **Obsidian 是唯一数据真源**：所有掌握度数字只来自伊菲真实作业落笔，不拍脑袋。
- **GitHub 是代码版本**：每次代码/配置变更都提交推送。
- 看板数据更新走「Obsidian → export_snapshot.py → seed_db.js」三步走，不要在控制台手改云端文档。
- `analyze_image`（AI 读图归因）目前由 WorkBuddy 自动化真实注入，不依赖小程序自身。

---

## 七、故障排查

| 现象 | 处理 |
|---|---|
| 小程序打开「掌握情况」空白 | 云函数没部署 / 环境 ID 不对 → 重跑第四节第 1 步；确认 `app.js` 里 envId |
| 上传作业报环境错误 | 确认 `cloudfunctions/*/index.js` 用 `DYNAMIC_CURRENT_ENV`（已写），且云函数部署到了 `cloud1-…` |
| `tcb` 报未登录 | 重跑 `tcb login --flow device` |
| 看板数字不对 | 改 Obsidian 对应 `00-最新状况.md` 的 YAML 块 → 重跑 export + seed |
